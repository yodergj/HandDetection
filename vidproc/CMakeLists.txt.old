PROJECT(HandDetection)
SET(Qt4_FIND_REQUIRED true)
FIND_PACKAGE(Qt4)
IF (QT4_FOUND)
#INCLUDE(${QT_USE_FILE})
  INCLUDE_DIRECTORIES(${QT_INCLUDES})
  QT4_WRAP_CPP(QT_MOC_SRCS Window.h)
  ADD_EXECUTABLE(VidProc Window.cpp VideoDecoder.cpp ${QT_MOC_SRCS})
  TARGET_LINK_LIBRARIES(VidProc gmm ${QT_LIBRARIES} avformat)
ENDIF (QT4_FOUND)
